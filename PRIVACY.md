# 隐私说明

## 本地保存的数据

扩展使用 `chrome.storage.local` 保存：

- 当前翻译通道；
- 千问和 DeepSeek 的 API 地址；
- 千问和 DeepSeek 的 API Key；
- 千问和 DeepSeek 的模型名称；
- 用户填写的 Midjourney 翻译要求；
- 用户主动新增或更新的提示词库记录，包括 ID、标题、中文、英文和日期；
- 最多 1,000 条按翻译通道隔离的页面翻译缓存；
- 画幅比例、风格化强度、混乱度、质量和 HD 开关。

这些数据不会同步到扩展开发者的服务器。浏览器本地存储不是系统级密钥保险箱，可访问本机 Chrome 配置目录或扩展调试环境的人可能读取其中内容。

## 发送到第三方的数据

只有当用户主动执行“翻译”“翻译并填入”“测试连接”或开启“翻译页面”时，所需文本才会被发送给当前选择的第三方服务：

- MyMemory 公共翻译 API；
- 用户配置的千问 API；
- 用户配置的 DeepSeek API。

开启“翻译页面”后，扩展会把当前 Midjourney 页面中符合条件的可见英文文本发送给所选服务进行英译中。这些文本可能包括界面标签和页面上可见的提示词。页面翻译默认关闭，仅在用户主动开启后运行。

页面翻译结果会写入有上限的本地去重缓存，以便相同英文在刷新或下次访问时不再重复发送。缓存按翻译通道隔离，最多保存 1,000 条，超出后淘汰最近最少使用的记录。扩展只向 MyMemory、千问、DeepSeek 或用户配置的接口发送完成当前翻译所需的文本。

Markdown 或 TXT 提示词文件只在浏览器本地解析和生成，不会上传到扩展开发者的服务器。导入时，相同 ID 的记录会在本地覆盖，不同 ID 的记录会新增。

在模型通道中，API Key 作为 Bearer 授权头发送到用户填写的 API 地址。在 MyMemory 免费通道中不发送 API Key。

## 不收集的数据

扩展不收集或保存：

- 完整的翻译请求历史；
- Midjourney 生成历史；
- 账户信息；
- 分析、广告或跟踪数据。

扩展不会出售用户数据，不显示广告，也不进行行为分析。

## 权限用途

- `storage`：保存本地配置。
- `tabs`：查找并激活已打开的 Midjourney 标签页，或新建官网标签页。
- Midjourney 网站权限：注入中文翻译面板、填充原生提示词输入框，并在用户主动开启后显示页面中文翻译。
- MyMemory 网站权限：调用免费翻译接口。
- 可选网站权限：只在用户保存或测试自定义模型地址时，请求访问该地址所在来源。
