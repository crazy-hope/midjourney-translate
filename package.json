{
  "name": "mj-prompt-translator",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free Chrome extension for translating Chinese Midjourney prompts.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate-extension.js"
  }
}
